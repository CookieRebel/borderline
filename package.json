{
  "name": "borderline-monorepo",
  "private": true,
  "workspaces": [
    "app",
    "backend",
    "website"
  ],
  "scripts": {
    "build": "npm run build:all",
    "build:all": "npm run build -w website && npm run build -w app",
    "db:migrate": "npm run db:migrate -w backend"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "prettier": "^3.0.0"
  }
}